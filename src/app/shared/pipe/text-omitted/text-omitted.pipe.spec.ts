import { TextOmittedPipe } from './text-omitted.pipe';

describe('TextOmittedPipe', () => {
  it('create an instance', () => {
    const pipe = new TextOmittedPipe();
    expect(pipe).toBeTruthy();
  });
});
