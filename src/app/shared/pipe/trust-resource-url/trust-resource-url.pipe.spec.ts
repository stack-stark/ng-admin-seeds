import { TrustResourceUrlPipe } from './trust-resource-url.pipe';

describe('TrustResourceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new TrustResourceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
